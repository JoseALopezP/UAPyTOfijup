'use client'
import styles from '../Menu.module.css'

export default function MenuImportantDates() {
    return (
        <section className={`${styles.importantDatesSection}`}>
            <h2>FECHAS IMPORTANTES</h2>
            <div className={`${styles.importantDatesBody}`}>
            </div>
        </section>
    )
}