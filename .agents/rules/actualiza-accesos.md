---
trigger: always_on
---

Debes tener en cuenta que al agregar partes actualices cuándo se crea la build si se tiene acceso o no a esa parte para cada una de las build distintas que se están realizando